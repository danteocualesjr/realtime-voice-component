[**realtime-voice-component**](../README.md)

---

# Type Alias: RealtimeTracing

```ts
type RealtimeTracing =
  | "auto"
  | {
      groupId?: string;
      metadata?: Record<string, unknown>;
      workflowName?: string;
    };
```
